[package]
name = "kontrolrgb-effects"
version = "0.1.0"
description = "Pure effect math, device types, and layout fixtures shared by the KontrolRGB app and the kontrolrgb-sim web preview"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
