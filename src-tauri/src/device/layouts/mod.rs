//! Keyboard layouts moved to the shared `kontrolrgb-effects` crate so the
//! kontrolrgb-sim web preview renders the exact same key geometry.

pub use kontrolrgb_effects::layouts::{gmmk_ansi, gmmk_tkl};
